移动端选择的按钮 比如购物车的不好选中  可以在外层再加一个div 设置padding 把点击事件绑定到div上面，增加点击范围

store 传参的时候要以键值对的方式传

使用keep-alive 的时候  获取参数要在 激活的状态下获取  如果使用到定时器  要记得在销毁激活状态的时候 停止


详情页面请求数据的时候  最好加一个判断  if(this.id!=id)时候在请求

使用组件库的时候  如果样式修改无效的话  可以在APP.vue里面修改